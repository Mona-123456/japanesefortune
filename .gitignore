node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Internal strategy / legal / art / design specs — keep local, do not publish
japanese-fortune-mvp-spec.md
hero-image-spec.md
docs/phase2-derivation-spec.md
docs/phase2-synthesis-spec.md

# Hero art masters (12–13MB each): archived locally in content/, not served.
# Inputs to `npm run build:hero`; the committed derivatives in assets/img/ ship.
content/hero-master-*.png

# Heavenly-Stem symbol masters (~1.5MB each): archived locally in content/.
# Inputs to `npm run build:symbols`; the committed stem-* derivatives ship.
content/10_symbols_*.png

# Zodiac (Earthly Branch) masters (j01–j13, ~2MB each): archived locally in content/.
# Inputs to `npm run build:zodiac`; the committed zodiac-* derivatives ship.
content/j??.png
