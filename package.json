{
  "name": "japanesefortune",
  "version": "0.1.0",
  "private": true,
  "description": "Japanese Fortune Telling site (Four Pillars / BaZi + Nine Star Ki). Static site, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "node --version >/dev/null && python -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
